import React from 'react';
import CreateProductsStep1 from '@app/(processes)/generate-custom-products/steps/1/page';

/**
 * Page component for the "Download Official Products" section.
 *
 * This component serves as the entry point for the download official products process,
 * rendering the first step of the process.
 *
 * @component
 * @returns {JSX.Element} The rendered page component for the "Download Official Products" section.
 */
export default async function CreateCustomProductsPage() {
	return <CreateProductsStep1 />;
}
