"use client"

import { useUserInfoCookie } from "../(auth)/_hooks/useUserInfoFromCookie"
import { useRedirectIf } from "../(shared)/_hooks/useRedirectIfNot"

// `app/page.tsx` is the UI for the `/` URL
export default function Page() {

	return <h1>results</h1>
}